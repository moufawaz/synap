# Dependencies
node_modules/
/.pnp
.pnp.js

# Production
/.next/
/out/
/build

# Env files — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

.vercel

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
*.ipa

# Android signing — NEVER commit keystores
*.jks
*.keystore

# iOS build output
ios/App/Pods/          # handled by CocoaPods; large
build/

# Capacitor generated files that CI rebuilds from source
# (keep android/ and ios/ committed — CI needs them)

# Local workspace artifacts
*.zip
*.docx
*.xlsx
*.txt
~$*
ChatGPT Image*.png
SYNAP_PROJECT_KNOWLEDGE.md
fix_*.mjs
handoff/
patch-for-synap-app/

# ── Secrets & credentials — NEVER commit (provisioning profiles → GitHub secrets only) ──
**/credentials.json
*.mobileprovision
*.p12
*.p8
tmp_*.json
